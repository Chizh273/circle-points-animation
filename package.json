{
  "name": "new-js-animation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "tsc && vite build --base=/circle-points-animation",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@types/dat.gui": "^0.7.12",
    "gh-pages": "^6.1.1",
    "prettier": "3.1.1",
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "dat.gui": "^0.7.9"
  }
}
